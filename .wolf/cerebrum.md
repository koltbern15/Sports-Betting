# Cerebrum

> OpenWolf's learning memory. Updated automatically as the AI learns from interactions.
> Do not edit manually unless correcting an error.
> Last updated: 2026-05-26

## User Preferences

<!-- How the user likes things done. Code style, tools, patterns, communication. -->

## Key Learnings

- **Project:** sports-betting
- **SBR HTML format:** Each page has 1 table, row 0 is headers, rows 1+ are data. Two rows per game (away first, home second). Open/Close columns are dual-use: one row holds the game O/U total (value >25), the other holds the home spread (value ≤25 or 'pk'). The assignment to V vs H row is NOT consistent — ~37% of games are "flipped". Parse strategy: classify by value size. ML column (index 11) holds American-format moneyline for each team's row — opening ML is always present.
- **SBR spread sign:** Always shown as a positive number. Use the home team's ML sign to determine direction: home ML negative = home is favorite = home spread is negative.
- **SBR season coverage:** 2007-08 through 2021-22 only (15 seasons). URL pattern: nfl-odds-{YYYY}-{YY}. No 2022+ data.
- **SBR date format:** Integer MMDD (e.g. 909 = Sep 9, 130 = Jan 30). Year must be inferred from the URL.
- **Aussportsbetting blocked:** The site (https://www.aussportsbetting.com) is behind Cloudflare WAF. All automated fetch attempts (urllib, WebFetch, full browser headers) return HTTP 403. The xlsx must be downloaded manually in a real browser. Do NOT attempt automated fetch; it will always fail.
- **Streamlit AppTest path resolution:** `AppTest.from_file(path)` resolves paths relative to the calling test file, NOT the project root. A test in `tests/` referencing `app/main.py` must use `../app/main.py`. (Slice 8, T5)

## Do-Not-Repeat

<!-- Mistakes made and corrected. Each entry prevents the same mistake recurring. -->
<!-- Format: [YYYY-MM-DD] Description of what went wrong and what to do instead. -->

- [2026-05-26] **Don't forward-reference imports in TDD plans.** Plan T2 included `import math` in `engine/stats_utils.py` because it would be used in T5 (Wilson CI). Ruff (F401) flagged it as unused at T2. Fix: each task's code block should only import what that task uses; subsequent tasks add their own imports. When adding helper modules in incremental TDD, scope imports to the function being added.
- [2026-05-27] **Floating-point equality in tests must use pytest.approx.** Slice 2 T1 plan specified `assert m.roi_neg110 == 0.15` for dollar_weighted_roi with payouts `[1.30,-1.0,1.30,-1.0]`. Real result is `0.15000000000000002` due to float math. Fix: use `pytest.approx(0.15)`. When writing future plans involving sum/divide of floats, default to `pytest.approx` even when the math looks like it should land on a clean value.
- [2026-05-27] **Unused-import gotcha recurred in TEST file (Slice 2 T3).** Plan's test_moneyline.py included `import math` but never used it; ruff F401 flagged it. Same root cause as the 2026-05-26 entry, but in tests, not source. Fix: removed the unused import; tests still pass. When executing a TDD plan, scan import blocks against actual usage in BOTH source and test files before running ruff.
- [2026-05-27] **DB column names: use the schema, not the Kaggle CSV column names.** The Slice 2 plan drafted SQL queries with `b.total_line` and `g.score_home`/`g.score_away` — these are *Kaggle CSV* column names. The actual SQLite schema (engine/db.py) uses `b.total_close`, `g.home_score`, `g.away_score`. Both T7 and T9 implementer prompts had to be corrected before dispatch. Fix: when writing any plan that references DB queries, read engine/db.py for the schema first. The Kaggle column → DB column mapping happens in ingestion/loader.py.
- [2026-05-27] **derive_ml_from_spread must clamp probabilities at extreme spreads.** Real Kaggle data has games with spread_home_close as extreme as -26.5, where `p * 1.04762` overround pushes the implied probability above 1.0 and crashes `_prob_to_american`. Original fix (`_EPS=1e-6` clamp) silently produced -99,999,900 — still wrong. [2026-05-29] **Correct fix:** clamp to `_MAX_IMPLIED_PROB = 10000/10100` / `_MIN_IMPLIED_PROB = 100/10100` so derived prices floor near +/-10000 American and heavy-fav payouts stay realistic (> 0.001 per unit). Any new probability-to-American helper anywhere in the codebase must handle the same edge case.
- [2026-05-27] **ruff E741: single-char `l` is ambiguous in test/aggregator loops.** Pattern recurred in T7 and T9: per-season unpacking like `for season, (w, l) in groups` trips E741. Fix: use `losses` (or `losses_` if `losses` already exists in outer scope). Don't disable E741 — rename.
- [2026-05-27] **Appending imports mid-file causes ruff E402.** T9 plan said APPEND tests to test_validation.py, which caused new `import sqlite3` etc. to land after function definitions — ruff E402. Fix: always move new imports to the top-of-file import block when appending tests to an existing test file. Also fix: plan's test assertion `assert "ml_heavy_fav" in bucket_names or "ml_mid_fav" in bucket_names` assumed fixture spreads that don't produce those buckets; actual fixture spreads (-3.0, -7.0, -3.0, -0.5) produce ml_big_fav/ml_small_fav/ml_slight_fav/ml_pickem/ml_small_dog/ml_mid_dog. Verify bucket assertions against actual fixture data before writing plan tests.

## Decision Log

<!-- Significant technical decisions with rationale. Why X was chosen over Y. -->

- [2026-06-13] **Post-audit hardening pass (45-agent verified audit → fixes).** Fixed 2 HIGH crashes (live_odds.parse_odds_payload now skip-and-counts malformed events like the AUS/SBR parsers; this_week_view.render() None-guards consensus spread/total). Dashboard honesty: the Finding tab's CLV↔result card is now COMPUTED LIVE via `data.clv_result_correlation` (scipy.stats.pearsonr over opener-graded bets, drop pushes) instead of a hardcoded "r≈0.12·p≈1e-14" literal — live value is r=0.1151, p=1.32e-14, n=4455 (matches the old literal, now can't drift); "Bets analyzed" sums spread+total (9,139) not spread-only; swing card selects CLV extremes by bucket LABEL not iloc position. CLV Explorer now surfaces a `low_n` (insufficient_sample, <50 decided) flag + an unadjusted-multiple-comparisons caveat. SBR `_classify_pair` now threads the Close column to resolve ambiguous Open pairs (both <=25 or both >25) and skips when unrecoverable (4 such games across 2007-2021, all correctly skipped). **Deliberately NOT changed:** (a) BucketMetrics roi_neg105 duplicate column — cosmetic, unread by any consumer, shared schema risk; (b) opening_line_loader (season,home,away) join without week — verifier proved 100% opener coverage, latent not active; (c) full build_board_artifact dedup refactor — hardened with None-guards instead (template consumes dict shapes, not GameOdds tuples, so reuse is a risky rewrite of a deliverable). ClvRow gained `insufficient_sample: bool = False` (defaulted so positional construction in tests stays valid).

- [2026-05-29] **Slice 6 ingested opening lines** — aussportsbetting (primary, manual download due to Cloudflare; spread+total+ML, decimal odds, sign matches our convention) + SBR (cross-check, 2007-2021, interleaved spread/total resolved by >25 rule, spread direction from home ML). ML came back IN via aussportsbetting. CLV is Slice 7. Overlap agreement spread 61%@tol0.5/75%@tol1.0, total 66%/82% — sub-100% is expected (two sources, different snapshot times). SBR _classify_pair can misfire when both Open values are <=25 (~26 rows); AUS is canonical for 2013+ so these don't affect CLV.
- [2026-05-29] **Slice 5 replaced the binary credible-edges gate with a continuous edge report + power/MDE columns** after an audit found the gate underpowered (Wilson-lower-bound gate needs +6-20% ROI to clear at available bucket sizes vs real edges of +1-3%).
- [2026-05-31] **Slice 9 historical showcase tabs: 5-tab Streamlit capstone.** Four showcase tabs added (The Finding, Edge Report, CLV Explorer, Data & Audit) alongside the live This Week board. Tab order: This Week leads. The Finding uses `grade_at='close'` for the open-vs-close proof panel (rising vs flat win-rate curves). CLV ladder rendered as Altair line+points — honest encoding, avoids truncated-bar distortion. designqc-driven polish applied throughout. 337 tests pass, ruff clean.
- [2026-05-30] **Slice 8 live-first pivot: live odds board over a historical simulator.** The board's real value is line shopping (best price per side across books = repeatable +1-5 cents per leg) and live line-movement signal, not the historical bucket stats. `ODDS_API_KEY` is resolved from env var or a gitignored `.env` file and must NEVER be committed. `data/raw/odds_api_latest.json` is gitignored (local preview/cache only). Live pull is gated on the user's API key; everything else (tests, board rendering with a seed file) works without it. Historical showcase tabs in the Streamlit UI deferred to Slice 9. Honesty rails: top-of-page banner explicitly frames the board as context + best price, not certified picks.
- [2026-05-30] **Slice 7 CLV engine: close IS sharper than the open.** Ran `engine.clv` on 4,570 spread + 4,569 total bets. Win rate rises PERFECTLY monotonically (no inversions) in both markets across all 5 CLV buckets. Positive-CLV buckets (clv_05_2, clv_gt_2) clear the 52.38% -110 breakeven in both markets. Spread clv_gt_2 (n=293): 57.59% win rate, ROI +9.84%, p=0.043. Total clv_gt_2 (n=305): 57.24%, ROI +9.03%, p=0.053. The tails are borderline underpowered (mde80 ~11-12% ROI vs observed ~10%); significance would firm up with ~3-4x the sample. The negative-CLV half is unambiguous (large n, clear below-breakeven win rates). CONCLUSION: the close is sharper than the open in this data — CLV is real signal. This validates the line-movement hypothesis but is NOT a strategy (CLV is unknown until close). ML-CLV follow-on (Slice 8) is warranted — data is available (5,144 AUS opening ML rows).
- [2026-05-26] **Project decomposed into slices.** Full NFL betting analytics spec is too large for one design/plan. Decomposition: Slice 1 = ingestion + schema + stats_utils + one ATS analysis; Slice 2 = rest of Phase 2 analytics; Slice 3 = static report; Slice 4 = Streamlit dashboard; Slice 5+ = FastAPI/live odds. Each slice gets its own spec → plan → build cycle.
- [2026-05-26] **Data source: Kaggle "NFL Scores and Betting Data" CSV first; scrapers deferred.** Reason: legal, stable, no scraper fragility, gets math moving immediately. Pro-Football-Reference / SportsOddsHistory / Covers will be layered in for opens, moneylines, line movement in Slice 2+.
- [2026-05-26] **Dependency mgmt: `uv` (not pip+venv).** Faster, lockfile, modern. `pyproject.toml` is single source of truth.
- [2026-05-26] **Database: SQLite via stdlib `sqlite3`; no ORM.** Scale doesn't justify SQLAlchemy. Postgres deferred until SaaS-prod slice.
- [2026-05-26] **ATS push handling: pushes return stake (0 PnL), excluded from win-rate denominator, included in total bets denominator for ROI.**
